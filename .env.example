# Example environment variables for local development
# Copy this file to `.env` and set secure values (do NOT commit real secrets)

# Admin password used to access admin pages in the Streamlit app
ADMIN_PASSWORD=6things
