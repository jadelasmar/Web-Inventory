# BIM POS Inventory System

A web-based inventory management system built with Streamlit.

## Features
- 📊 Dashboard with inventory overview
- 📋 View complete inventory
- ⚠️ Stock alerts for low inventory
- 🔄 Movement log tracking
- ➕ Add products (Admin only)
- 📦 Stock movement management (Admin only)

## Deployment

This app is deployed on Streamlit Community Cloud and accessible at: [Your-App-URL]

## Local Development

1. Clone the repository
2. Install dependencies: `pip install -r Requirements.txt`
3. Run the app: `streamlit run app.py`

## Database

Uses SQLite database (`bimpos_inventory.db`) for data storage.
